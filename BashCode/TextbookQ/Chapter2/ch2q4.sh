#!/usr/bin/bash
 
touch shopping_list
chmod 600 shopping_list

#ASSIGNMENT
#Write asequence of two commands that creates an empty file named
#shopping_list that you can read and write but that cannot be read or written by anyone else.

#NOTE: If you're referencing files in the Windows file system, they do not, by default, retain Linux permissions.
#These projects are in a windows file system "mnt/c/Users/mishi" to be exact
#So the permissions will not work
